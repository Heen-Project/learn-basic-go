package main

import "fmt"

func main() {
	fmt.Println("Hello World!")
}

// go build: compile the code
// go run: compile and executes the code
// go fmt: format all the code in each file in the current directory
// go install: compile and install a package
// go get: download the raw source of third party's package
// go test: run tests
